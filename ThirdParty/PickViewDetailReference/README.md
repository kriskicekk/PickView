# PickView Detail Reference

This directory contains the original PickView client detail-page source used as the migration reference for PickView's Mac detail window.

The copied upstream code is licensed under GPLv3. See `LICENSE`.
